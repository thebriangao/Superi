# P3.W07.C014

## Outcome

Implemented shared fail-closed handling for IME composition, dead keys, Process and Unidentified
events, legacy keyCode 229, and settled NFC non-Latin shortcuts.

## Architecture and integration

The shell input owner classifies unsettled input once. Global configurable shortcuts, the command
palette, and inline timeline track naming reuse that policy before routing commands, navigating,
executing, committing, or cancelling, while ordinary controlled inputs retain browser-native text
composition and settled Unicode shortcuts retain their canonical identity.

## Implementation process

A failing contract established modern and legacy composition cases, Unicode normalization, and real
editable consumer guards before the shared classifier and integrations were added.

## Verification

Thirty-three focused input-method, shortcut, shell, command-palette, and timeline contracts, 219
frontend tests, strict TypeScript, the 107-module production build, and codebase-map validation pass.

## Delivery

Input classification, real consumers, tests, evidence, and maps ship together.

## Remaining constraints

Physical IME certification remains part of each supported operating-system, keyboard, and input
method release matrix.
