# P3.W03.C009 offline media recovery flows

## Outcome

The desktop media library now exposes fresh online, partial, or offline source state, bounded local search, and revision-fenced relink, replace, and conform operations without any commercial service.

## Architecture and integration

The existing Tauri project-media store remains the persistence and mutation owner. Relink accepts only the current content fingerprint and preserves `MediaId`, attachments, freshness, quality choice, and deterministic fallback. Replace retains editorial identity while explicitly changing source fingerprint so the delivered C008 freshness path marks prior derived media stale. Conform changes only the locally stored source interpretation. The typed TypeScript bridge and existing React media browser consume the same replacement snapshot.

## Implementation process

Work remained source-read-only until C008 commit `ee952ad41f29659295fd691c3af153085d6286c9` was remotely contained. One frozen contract was added red first, then the native state, local bridge search, explicit controls, persistence, and command registration were implemented as one contained slice. C010 thumbnail, filmstrip, waveform, and preview generation remain outside this checkpoint.

## Verification

Fresh focused Rust proof passes `offline_media_contract` and the retained `derived_media_lifecycle_contract` with `CARGO_TARGET_DIR=/private/tmp/superi-targets/P3.W03.C009`. The real React consumer passes TypeScript checking and the production Vite build. Rust formatting and the repository codebase-map validator are final delivery gates.

## Delivery

The substantive implementation, frozen contract, current workspace map, and this durable record are delivered together in one checkpoint commit to `origin/main`.

## Remaining constraints

Relink and replace require caller-supplied fingerprints; content hashing remains with the existing local import and inspection owner. Conform currently owns exact frame-rate reinterpretation only, and later preview generation and indexed local-AI content search remain separately owned.
