# Canonical cargo-deny policy, enforced by the dependency-policy CI workflow.
# Permissive-only, ZERO copyleft (GPL/LGPL/AGPL/MPL all intentionally absent).

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "CC0-1.0",
    "ISC",
    "NCSA",
    "Zlib",
    "Unicode-DFS-2016",
    "Unicode-3.0",
]
confidence-threshold = 0.9

[bans]
multiple-versions = "warn"

[advisories]
yanked = "warn"

[sources]
unknown-git = "deny"
required-git-spec = "rev"
allow-git = [
    "https://github.com/OxideAV/oxideav-mp3",
]
