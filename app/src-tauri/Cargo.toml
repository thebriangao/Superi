[package]
name = "superi-desktop"
version = "0.0.0"
description = "Superi desktop shell"
edition = "2021"
license = "MIT"

[lib]
name = "superi_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
